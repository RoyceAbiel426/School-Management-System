# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGO_URL=mongodb://127.0.0.1:27017/OnlineSchool

# JWT Configuration
# Generate a strong secret key for production (at least 64 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-openssl-rand-base64-64
JWT_EXPIRES_IN=24h

# Rate Limiting Configuration
# Set to 'true' to completely disable rate limiting (not recommended for production)
DISABLE_RATE_LIMITING=false

# Upstash Redis Configuration
# Sign up at https://upstash.com to get these credentials
# Required for rate limiting (optional if DISABLE_RATE_LIMITING=true)
# Free tier: 10,000 requests/day
UPSTASH_REDIS_REST_URL=your-upstash-redis-rest-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token

# Frontend URL (for CORS)
# In production, set this to your actual frontend URL
FRONTEND_URL=http://localhost:5173

# Production Settings Example
# NODE_ENV=production
# FRONTEND_URL=https://your-domain.com
# MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/database
